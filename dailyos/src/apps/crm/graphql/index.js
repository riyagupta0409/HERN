import {
   CUSTOMERS_COUNT,
   TOTAL_REVENUE,
   CUSTOMER_ISTEST,
   COUPON_LISTING,
   CAMPAIGN_LISTING,
   COUPON_TOTAL,
   COUPON_DATA,
   CAMPAIGN_DATA,
   CAMPAIGN_TOTAL,
   CAMPAIGN_TYPE,
   REWARD_TYPE,
   REWARD_DATA_BY_COUPON_ID,
   REWARD_DATA_BY_CAMPAIGN_ID,
   BRAND_COUPONS,
   BRAND_CAMPAIGNS,
   WALLET_N_REFERRAL,
   SIGNUP_COUNT,
   LOYALTYPOINT_COUNT,
   BRAND_LISTING,
} from './subscriptions'
import {
   CUSTOMERS_LISTING,
   CUSTOMER_DATA,
   ORDERS_LISTING,
   REFERRAL_LISTING,
   WALLET_LISTING,
   LOYALTYPOINTS_LISTING,
   ORDER,
   ALL_DATA,
   STATUS,
   SUBSCRIPTION,
   SUBSCRIPTION_PLAN,
   OCCURENCES,
   REWARD_DATA,
   CUSTOMERS_LISTING_2,
} from './queries'

import {
   ISTEST,
   COUPON_ACTIVE,
   CAMPAIGN_ACTIVE,
   CREATE_COUPON,
   CREATE_CAMPAIGN,
   UPDATE_COUPON,
   UPDATE_CAMPAIGN,
   DELETE_COUPON,
   DELETE_CAMPAIGN,
   DELETE_REWARD,
   CREATE_REWARD,
   UPDATE_REWARD,
   UPSERT_BRAND_COUPON,
   UPSERT_BRAND_CAMPAIGN,
   CUSTOMER_ARCHIVED,
} from './mutations'

export {
   CUSTOMERS_COUNT,
   TOTAL_REVENUE,
   CUSTOMERS_LISTING,
   CUSTOMER_DATA,
   ORDERS_LISTING,
   ORDER,
   ALL_DATA,
   STATUS,
   SUBSCRIPTION,
   SUBSCRIPTION_PLAN,
   OCCURENCES,
   ISTEST,
   CUSTOMER_ISTEST,
   COUPON_LISTING,
   REFERRAL_LISTING,
   WALLET_LISTING,
   LOYALTYPOINTS_LISTING,
   COUPON_ACTIVE,
   COUPON_TOTAL,
   CREATE_COUPON,
   COUPON_DATA,
   UPDATE_COUPON,
   DELETE_COUPON,
   CAMPAIGN_TOTAL,
   CAMPAIGN_ACTIVE,
   CAMPAIGN_DATA,
   DELETE_CAMPAIGN,
   CREATE_CAMPAIGN,
   UPDATE_CAMPAIGN,
   CAMPAIGN_LISTING,
   CAMPAIGN_TYPE,
   REWARD_TYPE,
   CREATE_REWARD,
   UPDATE_REWARD,
   REWARD_DATA_BY_COUPON_ID,
   REWARD_DATA_BY_CAMPAIGN_ID,
   DELETE_REWARD,
   REWARD_DATA,
   BRAND_COUPONS,
   BRAND_CAMPAIGNS,
   UPSERT_BRAND_COUPON,
   UPSERT_BRAND_CAMPAIGN,
   CUSTOMER_ARCHIVED,
   WALLET_N_REFERRAL,
   SIGNUP_COUNT,
   LOYALTYPOINT_COUNT,
   BRAND_LISTING,
   CUSTOMERS_LISTING_2,
}
