const files = require('./file')
const git = require('./git')
const folders = require('./folder')
const database = require('./database')
const themeStoreDb = require('./themeStoreDb')

module.exports = {
   files,
   folders,
   git,
   database,
   themeStoreDb
}
